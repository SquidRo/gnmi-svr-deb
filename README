Steps to install gnmi-svr on DUT.

# On linux server:
  1. apt-get install python-stdeb

  2. git clone https://github.com/SquidRo/gnmi-svr-deb gnmi-svr-deb

  3. cd gnmi-svr-deb; ./build_deb.sh

  4. copy ../python-gnmi-svr_0.1-1_all.deb to DUT

# On DUT:
  5. pip install protobuf; pip install grpcio

  6. dpkg -i python-gnmi-svr_0.1-1_all.deb

